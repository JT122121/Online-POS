"""GoOnlinePOS AI Marketing Agent - modular package.

This package is entirely separate from the GoOnlinePOS application itself
(the repo root's *.html files, modules/, vendor/, etc.). Nothing in here
imports, executes, or modifies any GoOnlinePOS source file - scripts under
ai-marketing-agent/ only ever *read* those files as plain text to verify
feature claims (see scripts/analyze_product.py).
"""
