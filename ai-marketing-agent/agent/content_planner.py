"""
Content Planner (Phase 4 - NOT YET IMPLEMENTED).

Planned behavior: sequence generated ideas/scripts into a simple local
publishing calendar (JSON file under data/), so `run-agent` has an
ordered queue of "what's next" rather than picking randomly each time.
"""


def plan_content(*args, **kwargs):
    raise NotImplementedError(
        "Content Planner is planned for Phase 4 and has not been built yet."
    )
